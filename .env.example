# Required
CHROME_PROFILE_PATH=/path/to/chrome/profile

# Optional - Amazon Credentials (for ASIN-based book selection)
AMAZON_EMAIL=your@email.com
AMAZON_PASSWORD=yourpassword

# Optional - API Keys (for cloud OCR and TTS)
OPENAI_API_KEY=sk-...
UNREAL_SPEECH_API_KEY=...

# Optional - OCR Configuration
OUTPUT_DIR=./exports
OCR_ENGINE=livetext  # livetext, tesseract, openai, local-vision
OCR_LANG=eng
DELAY_MIN_MS=3000
DELAY_MAX_MS=5000

# Optional - Local Vision Model Configuration
LOCAL_VISION_MODEL=qwen2-vl-7b  # qwen2-vl-7b, qwen2-vl-72b, llama-3.2-vision-11b, pixtral-12b
LOCAL_VISION_QUANTIZATION=4bit  # 4bit, 8bit, fp16, none
LOCAL_VISION_DEVICE=auto  # auto, cuda, mps, cpu
LOCAL_VISION_CACHE_DIR=~/.cache/kindle-exporter/models
LOCAL_VISION_BATCH_SIZE=4
LOCAL_VISION_MAX_MEMORY_MB=8000

# Optional - Chrome Configuration
CHROME_EXECUTABLE_PATH=/path/to/chrome

# Optional - Post-processing
POST_HOOK=/path/to/script.sh
